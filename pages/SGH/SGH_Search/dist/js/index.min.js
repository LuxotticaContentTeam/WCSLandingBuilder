(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
"use strict";

var _utils = require("./utils");
window.ct_wow__search = {
  container: null,
  prod_list: null,
  placeholdersUtils: {
    firstCircle: {
      container: null,
      prodsCount: 15,
      radius: null
    },
    secondCircle: {
      container: null,
      prodsCount: 17
    }
  },
  init: function init() {
    console.log('WOW SEARCH INIT');
    this.prod_list = document.querySelector('.ct_wow__search__products_list');
    this.buildHtml();
    this.setPlaceholders();
    this.entry();
  },
  buildHtml: function buildHtml() {
    var _this = this;
    window.ct_wow__search__products.forEach(function (prod) {
      _this.prod_list.innerHTML += "\n      <li class=\"ct_wow_search__product\">\n          <a href=\"/".concat(prod.upc, "\">\n              <div class=\"ct_wow_search__img_container\">\n                  <img src=\"https://assets.sunglasshut.com/is/image/LuxotticaRetail/").concat(prod.upc, "__STD__shad__fr.png?impolicy=SGH_bgtransparent&width=640\" alt=\"\">\n              </div>\n          </a>\n      </li>\n      ");
    });
  },
  setPlaceholders: function setPlaceholders() {
    var _this2 = this;
    var setMainCircle = function setMainCircle(first) {
      var container = document.querySelector('#ct_wow__search .ct_wow__search__pos_placeholders__main_results ');
      if (first) {
        for (var i = 0; i < 3; i++) {
          container.innerHTML += "\n            <div class=\"ct_wow__search__pos_placeholders__main_result__placeholder\"></div>\n          ";
        }
      }
      var coordinates = (0, _utils.calcCoordinates)(3, container.clientWidth / 2, container.clientWidth / 2, container.clientWidth / 2);
      container.querySelectorAll('div').forEach(function (elem, i) {
        elem.style.top = coordinates[i].y + 'px';
        elem.style.left = coordinates[i].x + 'px';
      });
    };
    var setFirstCircle = function setFirstCircle(first) {
      var container = _this2.placeholdersUtils.firstCircle.container = document.querySelector('#ct_wow__search .ct_wow__search__pos_placeholders__first_circle ');
      if (first) {
        for (var i = 0; i < _this2.placeholdersUtils.firstCircle.prodsCount; i++) {
          container.innerHTML += "\n            <div class=\"ct_wow__search__pos_placeholders__first_circle__placeholder\"></div>\n          ";
        }
      }
      _this2.placeholdersUtils.firstCircle.radius = container.clientWidth / 2;
      var coordinates = (0, _utils.calcCoordinates)(_this2.placeholdersUtils.firstCircle.prodsCount, _this2.placeholdersUtils.firstCircle.radius, _this2.placeholdersUtils.firstCircle.radius, _this2.placeholdersUtils.firstCircle.radius);
      container.querySelectorAll('div').forEach(function (elem, i) {
        elem.style.top = coordinates[i].y + 'px';
        elem.style.left = coordinates[i].x + 'px';
      });
    };
    var setSecondCircle = function setSecondCircle(first) {
      var container = _this2.placeholdersUtils.secondCircle.container = document.querySelector('#ct_wow__search .ct_wow__search__pos_placeholders__second_circle ');
      if (first) {
        for (var i = 0; i < _this2.placeholdersUtils.secondCircle.prodsCount; i++) {
          container.innerHTML += "\n            <div class=\"ct_wow__search__pos_placeholders__second_circle__placeholder\"></div>\n          ";
        }
      }
      _this2.placeholdersUtils.secondCircle.radius = container.clientWidth / 2;
      var coordinates = (0, _utils.calcCoordinates)(_this2.placeholdersUtils.secondCircle.prodsCount, _this2.placeholdersUtils.secondCircle.radius, _this2.placeholdersUtils.secondCircle.radius, _this2.placeholdersUtils.secondCircle.radius);
      container.querySelectorAll('div').forEach(function (elem, i) {
        elem.style.top = coordinates[i].y + 'px';
        elem.style.left = coordinates[i].x + 'px';
      });
    };
    var refreshCoordinates = function refreshCoordinates() {
      setMainCircle(false);
      setFirstCircle(false);
      setSecondCircle(false);
    };
    setMainCircle(true);
    setFirstCircle(true);
    setSecondCircle(true);
    window.addEventListener('resize', refreshCoordinates);
  },
  entry: function entry() {
    // this.container.classList.add('in');
    document.body.style.overflow = 'hidden';
  }
};
document.addEventListener('DOMContentLoaded', function () {
  window.ct_wow__search.init();
});

},{"./utils":2}],2:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.calcCoordinates = void 0;
var calcCoordinates = function calcCoordinates(nSides, originX, originY, radius) {
  var points = [{
    x: originX,
    y: originY + radius
  }];
  var theta = Math.PI / 2;
  var dTheta = 2 * Math.PI / nSides;
  for (var i = 1; i < nSides + 1; i++) {
    theta += dTheta;
    points.push({
      x: (originX + radius * Math.cos(theta)).toFixed(2),
      y: (originY + radius * Math.sin(theta)).toFixed(2)
    });
  }
  return points;
};
exports.calcCoordinates = calcCoordinates;

},{}]},{},[1]);
